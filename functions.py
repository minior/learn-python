# def, = function in PHP
def plus_one(num) :
    return num + 1
new_num = plus_one(5)
print(new_num)

def plus_one(num) :
    print (num + 1)
plus_one(5) # both works. Getting a value to be used elsewhere versus printing as part of the fn